#pragma once
#ifndef CREATETHREAD_H
#define CREATETHREAD_H

/*
Write a program that creates two threads. Each thread should print a
message indicating that it is running, along with the thread ID. 
The main thread should wait for both threads to complete before exiting.
*/

//Question 1
void printThread();

#endif 
